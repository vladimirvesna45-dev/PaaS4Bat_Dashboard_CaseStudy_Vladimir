services:
  api:
    build: ./backend
    ports:
      - '8000:8000'
    environment:
      CORS_ORIGINS: http://localhost:8080,http://localhost:5173

  web:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ''
    ports:
      - '8080:80'
    depends_on:
      - api
